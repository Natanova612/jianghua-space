# 日志：2026-08-12 —— JD 匹配器上线验证 + 「问姜华」交互核心 PRD

> **日期**：2026-08-12  
> **状态**：⚠️ 改动未提交 git；dev 服务器已停止；**「问姜华」PRD 待用户 review，未动工**  
> **PRD 路径**：`docs/prd-ask-jianhua.md`

---

## 今日成果

### 1. JD 匹配器：真实 JD 验证通过（5 份）

| JD | 分数 | 判断质量 |
|----|------|---------|
| AI 产品运营（文旅方向） | 90 | 高度匹配，证据锚定准确 |
| 出行增长运营（司机端/补贴） | 68 | 值得深聊；指出司机端供给侧、SQL、城市级供需三个真实缺口 |
| B2B 商户成功（种子商户陪跑） | 52 | 部分匹配；咬住重度 CSM、案例素材包两个未做过的点 |
| 海外 C 端增长 | 48 | 部分匹配；行业错配、无海外社媒实操、无小语种——全对 |
| Java 后端（反向测试） | 10 | 直接不推荐，不硬凑 |

**结论**：评分有区分度、证据可追溯、差距敢直说。功能达标。

**上线前优化**：报告底部新增「发邮件给姜华」CTA（mailto 预填主题），仅 score ≥ 40 时显示——低匹配岗位不引导联系，与坦诚逻辑一致。

### 2. 联调中踩掉的坑（备忘）

- Moonshot 该账号仅有两个模型：`kimi-k2.6` / `kimi-k2.7-code`（moonshot-v1-8k 404）
- kimi-k2.6 **只允许 temperature=1**，传 0.3 报 400——接口代码已移除 temperature
- k2.6 响应延迟 44-60s（深思型模型），UI 文案已如实改为「约需 30-60 秒」
- profile.json 从 readFileSync 改为 import 打包（Vercel serverless 读不到磁盘文件）
- API key 在本地 `.env`（已 gitignore）；**上线前需在 Vercel 环境变量配置 `MOONSHOT_API_KEY`**
- 备选模型名可用环境变量 `MOONSHOT_MODEL` 覆盖

### 3. 战略升级：「问姜华」交互核心

用户基于匹配器效果提出站点级重构：**AI 交互模块从简历页深处提升为站点主体**——
- 岗位匹配（for 面试官）+ 合作可能性分析（for 客户/合伙人）+ 随便聊聊（for 所有人）= 一个对话核心、三个入口
- 陈列内容（案例/思考/关于/简历）保留为栏目层，走高级杂志编排
- 对话核心位置已定：**首页即对话**（卷首输入框，轻初始态，发送后展开）

**PRD 草案 v0.1 已完成**（`docs/prd-ask-jianhua.md`），含：双层架构、用户场景、首页结构、功能规格、护栏五红线、内容安排、运营细节、风险表、里程碑 M1-M5。

### 4. 等用户拍板的五个问题（PRD 第九节）

1. 对话日志存不存（建议：存，仅问题文本+意图，脱 IP，界面注明）
2. 灰度策略：BETA 直接上 or 藏一周
3. 卷首引导语：「山水已阅，不如开口。」是否对味
4. 分身人格：克制顾问风 vs 允许引用读书划线的个性风
5. /resume 旧匹配器：保留 or 并入对话版

### 5. 底稿优化线索（来自真实 JD 测试）

- JD-1 暴露：SQL/数据分析能力底稿未写明 → 若会 SQL 自助取数，补进 `profile.json`
- JD-2 暴露：CSM/商户陪跑缺口 → 第一个顾问陪跑案例落地后补进底稿对冲

---

## 当前站点功能全景（截至今日）

| 模块 | 状态 |
|------|------|
| 首页「一纸长卷」三层架构（画/字/人） | ✅ v8 定稿 |
| 双语基建 + 卷首题签导航 | ✅ |
| 名章（中山王篆 + 钤印质感） | ✅ |
| about / resume / contact / starfield 降级 | ✅ P1 全部完成 |
| JD 匹配器（/resume） | ✅ 已验证，带 CTA |
| 「问姜华」对话核心 | 📄 PRD 待 review |
| 案例详情页 /work（脱敏方案：自绘示意图+STAR 复盘） | ⏳ 待做 |
| /notes 内容引擎（文旅平话范式） | 📄 手册已定，待写稿 |

## 明日恢复点

1. 用户 review `docs/prd-ask-jianhua.md`（可能带修改意见回来）
2. 五个开放问题拍板 → PRD 定稿 → M1 动工（/api/chat 流式接口 + 首页对话区 + 三意图）
3. 预览：`cd ~/jianghua-space && npm run dev`（端口可能被旧进程占用，用 `-- --port 4322` 或先杀残留）
4. git 仍未提交——PRD 定稿后建议连同 P1+P2 成果一起提交一次
